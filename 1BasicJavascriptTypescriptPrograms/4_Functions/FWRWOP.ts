function calculating() : number //number is a return type with number value and without parameters  

{let a : number = 81;
let b : number = 97;

//console.log("The Sum of A and B is : " +(a+b));
//console.log(b);  //printing the value, not returning
return b;

}

calculating(); //we are calling the function with functionname

console.log(calculating()); //frist -->we are calling the function, second--> we are getting the return value